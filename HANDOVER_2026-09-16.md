# Session state, 2026-09-16

Written before a context compaction so nothing below has to be rediscovered.
Everything here is measured, not remembered.

## The board

`cut-manifests/v1.0.99.yaml`, **167 rows, 38 still blocking the cut.** Every one
of the 38 carries a measured reason. 22 PRs merged today. The count has not
fallen much because **a row stops blocking when its PR MERGES**, and the merge
queue is the constraint, not the fixing.

## The merge queue is the whole bottleneck

Measured: `main` requires ONE check, `CI Required Gate`, with
`strict: true`. **Only one PR can be up to date at a time**, so merging is
serial by construction. Two drivers running at once double the waste rather
than halving the time. See `docs/DRIVING_THE_MERGE_QUEUE.md` for all three
traps, each with its measurement.

**SPLIT AGREED WITH ARCHIE (session hr015-gaming-pc-15):**

- **Archie owns #2026 and upward.** He is driving. #2034 first.
- **This session owns #2024 and below**, and is OFF THE QUEUE ENTIRELY until he
  reports. No updates, no re-runs, no merges, including on our own numbers.
- **Neither touches the five conflicted**: 1988, 1980, 1970, 1958, 1818. They
  need a human decision, not a retry.

**#2034 matters most.** About a dozen PRs are red on one shared cause: a
mutation that materialised its "pre-fix" state as `git show origin/main`, which
stopped working the moment the fix merged. #2034 rebuilds the defect from the
file in front of it so it cannot rot again.

**No register collision.** Archie holds HR015 #924 and #927, both merged. This
session holds #957, `feature_prs` only, `dmg_cuts` untouched. Last tag is
v1.0.98, his. Neither pushes another without telling the other. **Andy has not
authorised a tag and 38 rows still block, so there is nothing to tag.**

## Andy's decisions, taken today

| question | decision |
|---|---|
| Does two-party consent block the DMG | **YES.** Registered as row 937. |
| Warn on crossing, or stop | **WARN.** Re-confirmed after being told recording is already BLOCKED outright when the jurisdiction is known at the start. |
| Unlabelled records: show or hide | **Fix the generator.** Done. Legacy records stay hidden. |
| Consent copy naming minors and privileged settings | **Include, concise, as sub-text.** Done at the geo prompt; the install consent already named both. |
| Counsel | None affordable. Build conservatively, document for later review. |

## What is documented now that was not this morning

- `docs/PRIVACY_LEVELS.md` : two privacy scales, opposite directions, both
  written `L<n>`. `L3` means "hide" on one and "share widely" on the other.
- `docs/CONSENT_JURISDICTION.md` : how jurisdiction is decided across three repos.
- `docs/COUNSEL_BRIEF_PRIVACY_POSTURE.md` : every engineering stance with its
  reason, so a solicitor reviews rather than discovers.
- `docs/DRIVING_THE_MERGE_QUEUE.md` : the three queue traps.
- `OPEN_COMMITMENTS.md` : what was promised, its state, and the command ANDY can
  run to check each without asking. All rows currently closed.

## Open PRs holding finished work

CM051: 2008 2009 2010 2012 2015 2017 2018 2019 2020 2023 2024 2026 2027 2028
2029 2030 2032 2033 2034. CM042: #23 (consent bypass, country fallback, picker,
all 51 US jurisdictions). HR015: #956 (ledger detector), #957 (licence ledger
rows).

## Where this session stopped

Both driving sessions were near their context limit when this was written, so
the next reader may be a fresh session rather than either of us.

**Nothing is uncommitted and nothing is unpushed.** Five worktrees, all clean:
`wt-b3` (board), `wt-doc` (privacy scales, counsel brief, merge-queue docs),
`wt-csv` (jurisdiction table), `wt-epipe` (mutation fix), `wt-944` (wiki button,
and row 1589).

**One branch is pushed with NO PR open, deliberately:**
`fix/1589-the-panel-reads-the-box-not-the-transcript`. Opening a PR fires about
150 workflows and the queue was already 334 deep. Open it when the pool is
clear. The work is finished and the commit message carries the reasoning.

**THE FIRST THING THE NEXT SESSION SHOULD DO** is ask Archie
(hr015-gaming-pc-15) where his batch got to, rather than starting a driver. The
split was: he owns #2026 upward, this session owns #2024 and below, neither
touches the five conflicted. Two drivers running at once actively destroy each
other's in-flight gates, and that is measured, not theory. See
`docs/DRIVING_THE_MERGE_QUEUE.md`.

## Still owed, and none of it is blocked on Andy

- A context denylist that DISABLES capture for minors and privileged settings.
  Asked against today, not prevented. Row 937 holds it.
- 41 US states have no recorded position; they fall back to "ask".
- UK and US workplace guidance: documents, not code.
- The last agent (rows 1143, 1167, 1550) had not reported when this was written.
