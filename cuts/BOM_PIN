# cuts/BOM_PIN -- generated by scripts/sync_cut_bom.sh. DO NOT HAND EDIT.
#
# Records which OS003 commit each vendored cuts/<version>/MUST_CONTAIN.tsv was
# taken from, and its hash as taken. tests/test_cut_bom_is_fresh.sh recomputes
# these and goes RED on any mismatch, so editing the vendored copy in CM051
# instead of fixing it in OS003 cannot pass quietly.
#
# WHY THE BOM IS VENDORED AT ALL. OS003 is PRIVATE and CM051's GITHUB_TOKEN
# cannot read it, so the DMG assembly on a hosted runner cannot fetch the BOM
# at cut time. Same constraint, same remedy, and the same blind spot as
# cuts/REGISTRY_PIN: this proves the copy matches a NAMED commit, NOT that the
# commit is still OS003 HEAD. That half is an OPERATOR check --
# scripts/sync_cut_bom.sh --check before tagging.
os003_repo	andygmassey/OS003-Ostler-Release
os003_sha	c696dc801331e32884a2d4ecdf55fb3493fbe332
os003_branch	main
cuts/v1.0.41/MUST_CONTAIN.tsv	2b2e0522d52914b93097803e9727011f08fc7694c5c922d45230fd379bac4a28
